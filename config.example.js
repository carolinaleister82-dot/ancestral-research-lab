// Copy this file to config.local.js. The local file is ignored by Git.
// Never place access tokens, passwords, client secrets, or refresh tokens here.
window.ARL_CONFIG = {
  familySearch: {
    appKey: "",
    environment: "integration",
    redirectUri: "http://localhost:4173/"
  }
};

